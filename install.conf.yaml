- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev

- shell:
    - command: ./homebrew_install.zsh
      stdin: true
      stdout: true
      stderr: true
